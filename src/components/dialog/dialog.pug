#dialog
  el-dialog(:title="title" :visible.sync="visible" :width="width" @close="close" center)
    slot
    span(slot="footer", class="dialog-footer")
      el-button(type="primary", @click="confirm") {{button}}
      //- el-button(type="primary", @click="close") 取 消